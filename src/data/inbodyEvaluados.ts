import { UserAccount } from '../types/inbody';

/**
 * Semilla local desactivada: el roster vive en Firebase.
 * Se mantiene el export vacío para no romper imports.
 */
export const EVALUADOS_INBODY_REALES: UserAccount[] = [];
